---
import {getUrl} from '../scripts/getUrl'
---
<div transition:animate="slide" class="nav-links">
  <a href={getUrl('/')}>Home</a>
  <a href={getUrl("/about")} data-astro-reload>About</a>
  <a href={getUrl("/blog")}>Blog</a>
  <a href={getUrl("/tags")}>Tags</a>
</div>
